export type ConfirmEmail = {
  hasEmail: boolean;
};

export type UserAuth = {
  email: string;
  password: string;
  nickname: string;
};

export type LoginData = {
  email: string;
  password: string;
};
